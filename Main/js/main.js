$(window).load(function() {
    'use strict';

/*    document.addEventListener('keydown', function(event) {
        if (event.keyCode == 32) {
            peopleBehindthePixels.init();
        }
    });*/

    peopleBehindthePixels.init();
});


